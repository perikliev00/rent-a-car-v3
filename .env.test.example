# Copy this file to .env.test in the repo root and fill in your values.
# .env.test is gitignored — never commit real passwords.
#
# Use a dedicated test database. Do not point this at rent_a_car.

DATABASE_URL=postgresql://postgres:your_password@localhost:5432/luxride_test
